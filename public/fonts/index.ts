/**
 * Font manifest for ScanPay.
 *
 * Drop your TTF/OTF font files into this directory and register them here so
 * they are picked up by Next.js. The default font is the system stack; swap
 * `var(--font-sans)` in `src/app/globals.css` to use a custom font.
 *
 * Supported formats: .ttf, .otf, .woff, .woff2
 *
 * Example:
 *   "Noto Sans Devanagari": {
 *     regular: "/fonts/NotoSansDevanagari-Regular.ttf",
 *     bold: "/fonts/NotoSansDevanagari-Bold.ttf",
 *   },
 */

export const fonts = {
  sans: {
    regular: null,
    bold: null,
  },
  devanagari: {
    regular: null,
    bold: null,
  },
};

export default fonts;